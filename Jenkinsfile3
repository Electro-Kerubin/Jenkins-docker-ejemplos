pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                sh '''
                    echo --version
                    git --version
                    curl --version
                '''
            }
        }
    }
}